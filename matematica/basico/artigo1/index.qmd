---
title: "Cálculo de Limites"
lang: pt
language:
  toc-title: "Índice"
filters:
  - watermark
watermark:
  text: "RASCUNHO"
  size: 1.5
  angle: 30
  color: "#FF0000"
  opacity: 0.1
description: "Conceitos fundamentais e exemplos práticos de cálculo de limites de funções."
date: 2025-05-08
categories: ['Matemática', 'Secundário']
format:
  html: default
  pdf: default
toc: true
code-fold: true
execute:
  echo: true
  warning: false
  message: false
  eval: true
bibliography: artigo1.bib
csl: apa.csl
nocite: "@*"
reference-title: "Referências"
---

::: {.content-visible when-format="html"}
<div style="display: flex; gap: 1rem; margin-bottom: 1rem;">
  <a href="./index.pdf" target="_blank" class="btn btn-primary">📄 Baixar PDF</a>
  <button onclick="window.print()" class="btn btn-outline-secondary">🖨️ Imprimir</button>
</div>
:::


O **cálculo de limites** é uma ferramenta central no estudo do comportamento de funções.

## Exemplo

$$
\lim_{x \to 2} (x^2 + 3x - 4)
$$

```{python}
import sympy as sp

x = sp.symbols('x')
f = x**2 + 3*x - 4
limite = sp.limit(f, x, 2)
limite
```

Gráfico da função:

```{python}
import numpy as np
import matplotlib.pyplot as plt

x_vals = np.linspace(0, 4, 100)
y_vals = x_vals**2 + 3*x_vals - 4

plt.plot(x_vals, y_vals, label="f(x)")
plt.axvline(2, color='r', linestyle='--', label="x = 2")
plt.axhline(limite.evalf(), color='g', linestyle=':', label="lim f(x) = " + str(limite))
plt.title("Função f(x) = x² + 3x - 4")
plt.xlabel("x")
plt.ylabel("f(x)")
plt.grid(True)
plt.legend()
plt.show()
```



## Referências

