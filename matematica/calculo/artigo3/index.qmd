---
title: "Teorema de Cauchy"
lang: pt
language:
  toc-title: "Índice"
filters:
  - watermark
watermark:
  text: "RASCUNHO"
  size: 1.5
  angle: 30
  color: "#FF0000"
  opacity: 0.1
description: "Versão generalizada do Teorema de Lagrange e sua aplicação prática em funções contínuas e deriváveis."
date: 2025-05-08
categories: ['Cálculo', 'Matemática']
format:
  html: default
  pdf: default
toc: true
code-fold: true
bibliography: artigo3.bib
csl: apa.csl
nocite: "@*"
reference-title: "Referências"
---

::: {.content-visible when-format="html"}
<div style="display: flex; gap: 1rem; margin-bottom: 1rem;">
  <a href="./index.pdf" target="_blank" class="btn btn-primary">📄 Baixar PDF</a>
  <button onclick="window.print()" class="btn btn-outline-secondary">🖨️ Imprimir</button>
</div>
:::

<div style="margin-top: 2rem;"></div>


# Artigo 3 - Teorema de Cauchy

Se $f$ e $g$ são contínuas em $[a,b]$ e deriváveis em $(a,b)$, então existe $c$ tal que:

$$
\frac{f'(c)}{g'(c)} = \frac{f(b) - f(a)}{g(b) - g(a)}
$$

## Exemplo com $f(x) = \ln(x)$ e $g(x) = x$

```{python}
import sympy as sp
x = sp.symbols('x')
f = sp.ln(x)
g = x
a, b = 1, sp.E

lado_direito = (f.subs(x, b) - f.subs(x, a)) / (g.subs(x, b) - g.subs(x, a))
lado_direito.evalf()
```

Visualização:

```{python}
import numpy as np
import matplotlib.pyplot as plt

x_vals = np.linspace(1, np.e, 200)
f_vals = np.log(x_vals)
g_vals = x_vals

plt.plot(x_vals, f_vals, label="f(x) = ln(x)")
plt.plot(x_vals, g_vals, label="g(x) = x")
plt.title("Funções f(x) e g(x) - Teorema de Cauchy")
plt.legend()
plt.grid(True)
plt.show()
```