---
title: "Derivadas"
lang: pt
language:
  toc-title: "Índice"
filters:
  - watermark
watermark:
  text: "RASCUNHO"
  size: 1.5
  angle: 30
  color: "#FF0000"
  opacity: 0.1
description: "Introdução ao conceito de derivada, interpretação geométrica e aplicações iniciais."
date: 2025-05-08
categories: ['Matemática', 'Secundário']
format:
  html: default
  pdf: default
toc: true
code-fold: true
execute:
  echo: true
  warning: false
  message: false
  eval: true
bibliography: artigo2.bib
csl: apa.csl
nocite: "@*"
reference-title: "Referências"
---

::: {.content-visible when-format="html"}
<div style="display: flex; gap: 1rem; margin-bottom: 1rem;">
  <a href="./index.pdf" target="_blank" class="btn btn-primary">📄 Baixar PDF</a>
  <button onclick="window.print()" class="btn btn-outline-secondary">🖨️ Imprimir</button>
</div>
:::

<div style="margin-top: 2rem;"></div>



# Artigo 2 - Definição de Derivada

A derivada de uma função no ponto $x = a$ é definida como:

$$
f'(a) = \lim_{h \to 0} \frac{f(a+h) - f(a)}{h}
$$

## Exemplo com $f(x) = x^2$ em $x = 2$

```{python}
import sympy as sp
x, h = sp.symbols('x h')
f = x**2
a = 2

quociente = (f.subs(x, a + h) - f.subs(x, a)) / h
derivada = sp.limit(quociente, h, 0)

quociente.simplify(), derivada
```

Visualização da reta tangente:

```{python}
import numpy as np
import matplotlib.pyplot as plt

x_vals = np.linspace(0, 4, 200)
f_vals = x_vals**2
tangente = 4*x_vals - 4  # derivada 4, passa por (2,4)

plt.plot(x_vals, f_vals, label='f(x) = x²')
plt.plot(x_vals, tangente, '--', label='Tangente em x=2')
plt.scatter([2], [4], color='red')
plt.title("Derivada como reta tangente")
plt.legend()
plt.grid(True)
plt.show()
```

## Referências